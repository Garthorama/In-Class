import React from 'react';
import Header from '../components/Header';

export default class Prσfile extends React.Component {
    render() {
        return (
            <Header title="You are looking good!" />
        );
    }
}